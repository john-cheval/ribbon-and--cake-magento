query SitemapBlogPosts($pageSize: Int, $currentPage: Int) {
  mpBlogPosts(
    action: "get_post_by_categoryId"
    categoryId: 18
    pageSize: $pageSize
    currentPage: $currentPage
  ) {
    items {
      url_key
      created_at
    }
  }
}
