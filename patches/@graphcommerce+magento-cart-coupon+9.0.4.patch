diff --git a/node_modules/@graphcommerce/magento-cart-coupon/ApplyCouponForm/ApplyCouponForm.tsx b/node_modules/@graphcommerce/magento-cart-coupon/ApplyCouponForm/ApplyCouponForm.tsx
index 0a86e18..06c45cf 100644
--- a/node_modules/@graphcommerce/magento-cart-coupon/ApplyCouponForm/ApplyCouponForm.tsx
+++ b/node_modules/@graphcommerce/magento-cart-coupon/ApplyCouponForm/ApplyCouponForm.tsx
@@ -16,7 +16,7 @@ const { classes } = extendableComponent(name, parts)
 export function ApplyCouponForm(props: ApplyCouponFormProps) {
   const { sx = [] } = props
   const form = useFormGqlMutationCart(ApplyCouponFormDocument)
-  const { handleSubmit, control, formState, required, error } = form
+  const { handleSubmit,muiRegister, control, formState, required, error } = form
   const submitHandler = handleSubmit(() => {})
 
   return (
@@ -28,9 +28,10 @@ export function ApplyCouponForm(props: ApplyCouponFormProps) {
       sx={[
         (theme) => ({
           display: 'grid',
-          alignItems: 'center',
-          gridTemplateColumns: `1fr minmax(min-content, ${responsiveVal(70, 140)})`,
-          gridColumnGap: theme.spacings.sm,
+          alignItems: 'stretch',
+         // gridTemplateColumns: `1fr minmax(min-content, ${responsiveVal(70, 140)})`,
+         gridTemplateColumns: '8fr 4fr',
+          gridColumnGap: '13px',
         }),
         ...(Array.isArray(sx) ? sx : [sx]),
       ]}
@@ -40,21 +41,39 @@ export function ApplyCouponForm(props: ApplyCouponFormProps) {
         type='text'
         error={!!formState.errors.couponCode || !!error}
         required={required.couponCode}
-        name='couponCode'
+        // name='couponCode'
         rules={{ required: required.couponCode }}
         control={control}
         helperText={formState.errors.couponCode?.message}
         disabled={formState.isSubmitting}
         showValid
+        placeholder='Enter Coupon Code'
+        {...muiRegister('couponCode',{required:required.couponCode})}
+        sx={{
+    '& .MuiOutlinedInput-root': {
+      '& fieldset': {
+        borderColor: '#F6DBE0', 
+      },
+      '&:hover fieldset': {
+        borderColor: '#F6DBE0',
+      },
+      '&.Mui-focused fieldset': {
+        borderColor: '#F6DBE0', 
+      },
+    },
+  }}
       />
-      <FormControl>
+      <FormControl sx={{height:'100%'}}>
         <Button
           type='submit'
           className={classes.button}
           loading={formState.isSubmitting}
           color='secondary'
           variant='pill'
-          sx={{ whiteSpace: 'nowrap' }}
+          sx={{ whiteSpace: 'nowrap' , borderRadius:'4px',height:'100%', backgroundColor:'#F6DBE0', color:'#441E14',fontSize:{xs:'12px', sm:'14px',md:'16px'}, fontWeight:500,border:'1px solid #f6dbe0',lineHeight:'158%',boxShadow:'none !important','&:hover':{
+            backgroundColor:'transparent !important',
+            boxShadow:'none !important'
+          }}}
         >
           <Trans id='Apply' />
         </Button>
diff --git a/node_modules/@graphcommerce/magento-cart-coupon/CouponAccordion/CouponAccordion.tsx b/node_modules/@graphcommerce/magento-cart-coupon/CouponAccordion/CouponAccordion.tsx
index cefe300..d59b072 100644
--- a/node_modules/@graphcommerce/magento-cart-coupon/CouponAccordion/CouponAccordion.tsx
+++ b/node_modules/@graphcommerce/magento-cart-coupon/CouponAccordion/CouponAccordion.tsx
@@ -7,7 +7,7 @@ import {
 } from '@graphcommerce/next-ui'
 import { Trans } from '@lingui/react'
 import type { SxProps, Theme } from '@mui/material'
-import { Accordion, AccordionDetails, AccordionSummary } from '@mui/material'
+import { Accordion, AccordionDetails, AccordionSummary, Box } from '@mui/material'
 import { useState } from 'react'
 import { ApplyCouponForm } from '../ApplyCouponForm/ApplyCouponForm'
 import { RemoveCouponForm } from '../RemoveCouponForm/RemoveCouponForm'
@@ -36,7 +36,25 @@ export function CouponAccordion(props: CouponAccordionProps) {
   const handleChange = () => setOpen(!coupon && !open)
 
   return (
-    <Accordion
+
+    <>
+    <Box sx={{marginTop:{xs:'15px', sm:"20px", nd:'35px', lg:'40px'}}}>
+      <ApplyCouponForm  sx={{
+        '& .MuiInputBase-input':{
+          color:'#441E14',
+          padding:'20px 24px',
+          fontSize:{xs:'12px', sm:'14px',md:'16px'},
+          fontWeight:500,
+          lineHeight:'158%',
+          borderRadius:'4px',
+          '&::placeholder': {
+        color: '#441E14', 
+        opacity: 1, 
+      },
+        }
+      }} />
+    </Box>
+    {/*  <Accordion
       className={classes.accordion}
       onChange={handleChange}
       expanded={!coupon && open}
@@ -79,6 +97,9 @@ export function CouponAccordion(props: CouponAccordionProps) {
       <AccordionDetails sx={(theme) => ({ px: theme.spacings.xs })}>
         <ApplyCouponForm />
       </AccordionDetails>
-    </Accordion>
+    </Accordion> */}
+    </>
+   
+
   )
 }
