diff --git a/node_modules/@graphcommerce/magento-wishlist/components/ProductWishlistChip/ProductWishlistIconButton.tsx b/node_modules/@graphcommerce/magento-wishlist/components/ProductWishlistChip/ProductWishlistIconButton.tsx
index ad0f044..49b33bd 100644
--- a/node_modules/@graphcommerce/magento-wishlist/components/ProductWishlistChip/ProductWishlistIconButton.tsx
+++ b/node_modules/@graphcommerce/magento-wishlist/components/ProductWishlistChip/ProductWishlistIconButton.tsx
@@ -18,6 +18,7 @@ export type ProductWishlistChipProps = ProductListItemFragment & {
   sx?: SxProps<Theme>
   buttonProps?: IconButtonProps
   iconSvgProps?: Partial<IconSvgProps>
+  bigButton?:boolean
 }
 
 const compName = 'ProductWishlistChipBase'
@@ -25,7 +26,7 @@ const parts = ['root', 'wishlistIcon', 'wishlistIconActive', 'wishlistButton'] a
 const { classes } = extendableComponent(compName, parts)
 
 export const ProductWishlistIconButton = React.memo<ProductWishlistChipProps>((props) => {
-  const { buttonProps, iconSvgProps, sx = [], ...product } = props
+  const { buttonProps, iconSvgProps, sx = [],bigButton = false , ...product } = props
   const enabled = useWishlistEnabled()
   const { current, onClick, cancelBubble, showSuccess, hideShowSuccess } =
     useAddProductToWishlistAction({ product, index: 0 })
@@ -34,14 +35,20 @@ export const ProductWishlistIconButton = React.memo<ProductWishlistChipProps>((p
 
   return (
     <Box>
-      <IconButton
+      {bigButton ? (
+        <Button   onClick={onClick} sx={sx}>Buy Now</Button>
+      ): <IconButton
         onClick={onClick}
-        onMouseDown={cancelBubble}
-        onTouchStart={cancelBubble}
+        // onMouseDown={cancelBubble}
+        // onTouchStart={cancelBubble}
         size='small'
         className={classes.wishlistButton}
         {...buttonProps}
-        sx={[(theme) => ({ padding: theme.spacings.xxs }), ...(Array.isArray(sx) ? sx : [sx])]}
+        sx={[(theme) => ({  backgroundColor:(theme:any)=>theme.palette.primary.contrastText, padding:'10px ', backdropFilter:'blur(4px)',marginTop:{sm:'10px',md:'10px'}, marginRight:{sm:'10px',md:'10px'},'&:hover':{
+          backgroundColor:(theme:any)=>theme.palette.primary.contrastText,
+        },'&:focus':{
+          backgroundColor:(theme:any)=>theme.palette.primary.contrastText,
+        } }), ...(Array.isArray(sx) ? sx : [sx])]}
         title={
           current ? i18n._(/* i18n */ 'Remove from wishlist') : i18n._(/* i18n */ 'Add to wishlist')
         }
@@ -54,7 +61,7 @@ export const ProductWishlistIconButton = React.memo<ProductWishlistChipProps>((p
             src={iconHeart}
             size='medium'
             className={classes.wishlistIconActive}
-            sx={(theme) => ({ color: theme.palette.primary.main, fill: 'currentcolor' })}
+            sx={(theme) => ({ color: (theme:any)=>theme.palette.custom.wishlistColor, fill: (theme:any)=>theme.palette.custom.wishlistColor ,stroke: (theme:any)=>theme.palette.custom.wishlistColor})}
             {...iconSvgProps}
           />
         ) : (
@@ -63,10 +70,9 @@ export const ProductWishlistIconButton = React.memo<ProductWishlistChipProps>((p
             size='medium'
             className={classes.wishlistIcon}
             sx={(theme) => ({
-              color:
-                theme.palette.mode === 'light'
-                  ? theme.palette.text.secondary
-                  : theme.palette.background.paper,
+             
+              stroke: (theme:any)=>theme.palette.custom.wishlistColor,
+              fontSize:'18px',
               '.SidebarGallery-root &': {
                 // todo
                 color:
@@ -78,7 +84,8 @@ export const ProductWishlistIconButton = React.memo<ProductWishlistChipProps>((p
             {...iconSvgProps}
           />
         )}
-      </IconButton>
+      </IconButton>}
+     
 
       {import.meta.graphCommerce.wishlistShowFeedbackMessage && (
         <MessageSnackbar
diff --git a/node_modules/@graphcommerce/magento-wishlist/components/WishlistFab/WishlistFab.tsx b/node_modules/@graphcommerce/magento-wishlist/components/WishlistFab/WishlistFab.tsx
index 4c94637..3741f15 100644
--- a/node_modules/@graphcommerce/magento-wishlist/components/WishlistFab/WishlistFab.tsx
+++ b/node_modules/@graphcommerce/magento-wishlist/components/WishlistFab/WishlistFab.tsx
@@ -6,10 +6,11 @@ import React from 'react'
 import { useWishlistEnabled, useWishlistItems } from '../../hooks'
 
 export type WishlistFabContentProps = {
-  icon?: React.ReactNode
+  icon?: React.ReactNode | any
   FabProps?: Omit<FabPropsType, 'children'>
   BadgeProps?: BadgeProps
   sx?: SxProps<Theme>
+  length?:number
   activeWishlist: boolean
 }
 
@@ -18,7 +19,7 @@ const parts = ['root'] as const
 const { classes } = extendableComponent(name, parts)
 
 function WishlistFabContent(props: WishlistFabContentProps) {
-  const { icon, FabProps, BadgeProps, sx, activeWishlist } = props
+  const { icon, FabProps, BadgeProps, sx, activeWishlist ,length} = props
 
   const wishlistIcon = icon ?? <IconSvg src={iconHeart} size='large' />
 
@@ -35,11 +36,41 @@ function WishlistFabContent(props: WishlistFabContentProps) {
     >
       <NoSsr fallback={wishlistIcon}>
         {activeWishlist ? (
-          <DesktopHeaderBadge color='primary' variant='dot' overlap='circular' {...BadgeProps}>
+          <DesktopHeaderBadge sx={{
+    '& .MuiBadge-badge': { 
+      backgroundColor: '#FF0000',
+      color:'#fff',
+      display:'flex',
+      alignItems:'center',
+     justifyContent:'center',
+     textAlign:'center',
+     fontSize:'9px',
+     minWidth:'12px',
+     height:'14px',
+      right:'3px',
+     top:'4px'
+    },
+  }}  badgeContent={length} overlap='circular' {...BadgeProps}>
             {wishlistIcon}
           </DesktopHeaderBadge>
         ) : (
-          wishlistIcon
+          <DesktopHeaderBadge sx={{
+    '& .MuiBadge-badge': { 
+      backgroundColor: '#FF0000',
+      color:'#fff',
+      display:'flex',
+      alignItems:'center',
+     justifyContent:'center',
+     textAlign:'center',
+     fontSize:'9px',
+     minWidth:'12px',
+     height:'14px',
+      right:'3px',
+     top:'4px'
+    },
+  }}  badgeContent={length} overlap='circular' {...BadgeProps}>
+            {wishlistIcon}
+          </DesktopHeaderBadge>
         )}
       </NoSsr>
     </Fab>
@@ -54,10 +85,11 @@ export function WishlistFab(props: WishlistFabProps) {
 
   if (!enabled) return null
   const activeWishlist = wishlist.items.length > 0
+  const activeWishlistLength = wishlist.items.length
 
   return (
     <NoSsr fallback={<WishlistFabContent {...props} activeWishlist={false} />}>
-      <WishlistFabContent {...props} activeWishlist={activeWishlist} />
+      <WishlistFabContent {...props} activeWishlist={activeWishlist} length={activeWishlistLength} />
     </NoSsr>
   )
 }
diff --git a/node_modules/@graphcommerce/magento-wishlist/components/WishlistItem/AddWishlistItemToCart.tsx b/node_modules/@graphcommerce/magento-wishlist/components/WishlistItem/AddWishlistItemToCart.tsx
index a533979..d493c5e 100644
--- a/node_modules/@graphcommerce/magento-wishlist/components/WishlistItem/AddWishlistItemToCart.tsx
+++ b/node_modules/@graphcommerce/magento-wishlist/components/WishlistItem/AddWishlistItemToCart.tsx
@@ -10,13 +10,19 @@ import {
 import type { InputMaybe } from '@graphcommerce/next-config'
 import { Box } from '@mui/material'
 
-export type AddWishlistItemToCartProps = UseAddProductsToCartActionProps &
+export type customwislistButtonProps ={
+  showIcon?:boolean
+  isConfigurable?:boolean
+}
+
+export type AddWishlistItemToCartProps = UseAddProductsToCartActionProps & customwislistButtonProps &
   AddToCartItemSelector & { selectedOptions?: InputMaybe<string[]> | undefined }
 
 export function AddWishlistItemToCart(props: AddWishlistItemToCartProps) {
-  const { product, selectedOptions, index = 0, sku } = props
+  const { product, selectedOptions, index = 0, sku, showIcon,isConfigurable } = props
   const { setValue } = useFormAddProductsToCart()
 
+  
   return (
     <Box sx={{ display: 'flex' }}>
       <AddProductsToCartQuantity variant='standard' InputProps={{ disableUnderline: true }} />
@@ -27,7 +33,11 @@ export function AddWishlistItemToCart(props: AddWishlistItemToCartProps) {
         color='primary'
         variant='text'
         size='medium'
+        isIcon={showIcon}
+        icConfigured={isConfigurable}
+        urlKey={(product as any)?.url_key}
       />
+      
     </Box>
   )
 }
diff --git a/node_modules/@graphcommerce/magento-wishlist/components/WishlistItemActionCard/WishlistItemActionCard.tsx b/node_modules/@graphcommerce/magento-wishlist/components/WishlistItemActionCard/WishlistItemActionCard.tsx
index 81c7382..74fd973 100644
--- a/node_modules/@graphcommerce/magento-wishlist/components/WishlistItemActionCard/WishlistItemActionCard.tsx
+++ b/node_modules/@graphcommerce/magento-wishlist/components/WishlistItemActionCard/WishlistItemActionCard.tsx
@@ -6,11 +6,12 @@ import type { ActionCardProps } from '@graphcommerce/next-ui'
 import { ActionCard, actionCardImageSizes, extendableComponent } from '@graphcommerce/next-ui'
 import { Trans } from '@lingui/react'
 import type { SxProps, Theme, ButtonProps } from '@mui/material'
-import { Button, Link } from '@mui/material'
+import { Box, Button, Link } from '@mui/material'
 import type { ReactNode } from 'react'
 import { useRemoveProductsFromWishlist } from '../../hooks'
 import type { WishlistItemFragment } from '../../queries/WishlistItem.gql'
 import { AddWishlistItemToCart } from '../WishlistItem/AddWishlistItemToCart'
+import { RiDeleteBin6Line } from "react-icons/ri";
 
 export type WishlistItemActionCardProps = {
   item: WishlistItemFragment
@@ -19,6 +20,7 @@ export type WishlistItemActionCardProps = {
   isConfigurableUncompleted?: boolean
   secondaryAction?: ReactNode
   actionButtonProps?: ButtonProps
+  isIcon:boolean
 } & OwnerState &
   Omit<ActionCardProps, 'value' | 'image' | 'price' | 'title' | 'action'>
 type OwnerState = { withOptions?: boolean }
@@ -51,6 +53,7 @@ export function WishlistItemActionCard(props: WishlistItemActionCardProps) {
     secondaryAction,
     variant = 'default',
     actionButtonProps,
+    isIcon,
     ...rest
   } = props
   const { id, product } = item
@@ -128,11 +131,10 @@ export function WishlistItemActionCard(props: WishlistItemActionCardProps) {
               layout='fill'
               src={product?.small_image?.url}
               sx={{
-                objectFit: 'contain',
+                objectFit: 'cover',
                 width: actionCardImageSizes[size],
                 height: actionCardImageSizes[size],
                 display: 'block',
-                borderRadius: 1,
               }}
               sizes={actionCardImageSizes[size]}
             />
@@ -157,23 +159,59 @@ export function WishlistItemActionCard(props: WishlistItemActionCardProps) {
           )
         }
         secondaryAction={
-          secondaryAction ||
+          
           ((product?.__typename === 'SimpleProduct' ||
-            product?.__typename === 'VirtualProduct') && (
-            <AddWishlistItemToCart product={product} selectedOptions={[]} />
+            product?.__typename === 'ConfigurableProduct') && (
+              <Box sx={{
+                '& .MuiButtonBase-root':{
+                  width:'40px',
+                  height:'40px',
+                  '& svg':{
+                    fontSize:'26px !important',
+                    left:'7px'
+                  }
+                }
+              }}>
+            <AddWishlistItemToCart product={product} selectedOptions={[]} showIcon={isIcon} isConfigurable={product?.__typename === 'ConfigurableProduct'}/>
+            <Box sx={{
+            ['& span']:{
+              fontSize:{xs:'16px', lg:'20px'},
+              lineHeight:"24px",
+              fontWeight:"700",
+              color:(theme:any)=>theme.palette.custom.dark,
+              ['&:first-child']:{
+                paddingRight:"6px"
+              }
+            }
+          }}><Money {...product?.price_range.minimum_price.final_price} /></Box>
+            </Box>
           ))
         }
-        price={<Money {...product?.price_range.minimum_price.final_price} />}
+        price={<Box sx={{
+            ['& span']:{
+              fontSize:{xs:'16px', lg:'20px'},
+              lineHeight:"24px",
+              fontWeight:"700",
+              color:(theme:any)=>theme.palette.custom.dark,
+              ['&:first-child']:{
+                paddingRight:"6px"
+              }
+            }
+          }}><Money {...product?.price_range.minimum_price.final_price} /></Box>}
         action={
           <Button
             variant='inline'
             color='secondary'
             size='medium'
-            type='button'
+            type='button'sx={{
+              '& svg':{
+                color:'#333'
+              }
+            }}
             onClick={() => remove([id])}
             {...actionButtonProps}
           >
-            <Trans id='Remove' />
+           {/* <Trans id='Removes' /> */} <RiDeleteBin6Line />
           </Button>
         }
         size={size}
diff --git a/node_modules/@graphcommerce/magento-wishlist/hooks/useRemoveProductsFromWishlist/useRemoveProductsFromWishlist.tsx b/node_modules/@graphcommerce/magento-wishlist/hooks/useRemoveProductsFromWishlist/useRemoveProductsFromWishlist.tsx
index d62cdaf..b50d2e2 100644
--- a/node_modules/@graphcommerce/magento-wishlist/hooks/useRemoveProductsFromWishlist/useRemoveProductsFromWishlist.tsx
+++ b/node_modules/@graphcommerce/magento-wishlist/hooks/useRemoveProductsFromWishlist/useRemoveProductsFromWishlist.tsx
@@ -5,6 +5,8 @@ import { useEventCallback } from '@mui/material'
 import { UseWishlistGuestDocument } from '../useWishlistitems'
 import type { UseRemoveProductsFromWishlistMutationVariables } from './UseRemoveProductsFromWishlist.gql'
 import { UseRemoveProductsFromWishlistDocument } from './UseRemoveProductsFromWishlist.gql'
+import { WishlistItemFragment } from '@graphcommerce/magento-wishlist/queries/WishlistItem.gql'
+
 
 export function useRemoveProductsFromWishlist() {
   const client = useApolloClient()
@@ -39,7 +41,7 @@ export function useRemoveProductsFromWishlist() {
               __typename: 'Wishlist',
               id: 'guest-wishlist',
               items_count: items?.length,
-              items_v2: { __typename: 'WishlistItems', items },
+              items_v2: { __typename: 'WishlistItems', items:items as WishlistItemFragment[] },
             },
           ],
         },
